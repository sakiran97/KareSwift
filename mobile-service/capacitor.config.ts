import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.mobileservice',
  appName: 'mobile-service',
  webDir: 'dist/mobile-service/browser'
};

export default config;
