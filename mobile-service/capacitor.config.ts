import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.mobileservice',
  appName: 'mobile-service',
  webDir: 'www'
};

export default config;
