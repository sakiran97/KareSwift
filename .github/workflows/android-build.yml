name: Build Android APK

on:
  workflow_dispatch: # Allows manual triggering from GitHub UI
  push:
    branches: [ "main" ]
    paths:
      - 'mobile-service/**' # Only trigger if mobile app code changes

jobs:
  build-android:
    name: Build Android APK
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '21'

      - name: Install NPM Dependencies
        working-directory: ./mobile-service
        run: npm install

      - name: Build Angular App
        working-directory: ./mobile-service
        run: npm run build

      - name: Sync Capacitor
        working-directory: ./mobile-service
        run: npx cap sync android

      - name: Build APK (Debug)
        working-directory: ./mobile-service/android
        run: |
          chmod +x ./gradlew
          ./gradlew assembleDebug

      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: kareswift-app-debug
          path: mobile-service/android/app/build/outputs/apk/debug/app-debug.apk
          retention-days: 7
